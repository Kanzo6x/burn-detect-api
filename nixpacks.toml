[setup]
nixPkgs = ["python312"]
cmds = ["python3 -m ensurepip --default-pip"]

[build]
cmds = ["python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn -b 0.0.0.0:5000 run:flask_app"
