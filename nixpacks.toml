[phases.setup]
nixPkgs = ["python312", "pip"]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "gunicorn -b 0.0.0.0:8000 run:flask_app"
